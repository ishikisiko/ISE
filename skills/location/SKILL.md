# Nearby Places

Use `nearby_places` to find a place type or named business around an explicit
reference location, such as `nearest KFC to HKUST` or `香港科技大学附近的医院`.

Do not use implicit device location. `coffee shops near me` and `nearest
hospital` lack a deterministic reference point and must fall back to general
search or ask for a location.
