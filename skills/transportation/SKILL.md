# Route Directions

Use `route_directions` only when the user supplies both an origin and a
destination, for example `从上海到苏州怎么走` or `drive from SFO to San Jose`.

Do not use it for train departure times, flight schedules, or traffic around a
single unnamed location. Those requests need a timetable/search source that
this skill does not provide.
