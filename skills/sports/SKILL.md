# Sports Schedule

Use `sports_schedule` for the next or previous event of an explicitly named
team, or for a specifically named event. Examples include `next Golden State
Warriors game` and `Lakers vs Celtics score`.

Do not use it for general sports news, rules, player analysis, or a request that
does not identify a team/event. The free provider can return fewer events than
premium feeds, so report only the event actually returned.
